# Rebuilt on demand from the data tree (sub-second), stamped with the repo
# HEAD sha, and 48M — never commit it.
tools/.cdda-index.pickle

# ctags index, built on demand per CLAUDE.md.
.tags

# Per-session hook scratch: what memory looked like at session start.
# Meaningless to any other session, and prunes itself after a week.
.state/
